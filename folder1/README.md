hello

Good bye

repository, branches, commits, tags

---
# create the repo locally
local repository -> "remote repository (git add remote <name> Murl>)

---
# create the repo remotely
git clone <url>

# create a brach locally
git checkout <branch>
git checkout -b <new branch>
git add ...; git commit -m '...'
git push --set--upstream origin <new branch>
git pull [origin <new brach>]